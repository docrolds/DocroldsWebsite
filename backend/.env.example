# Backend Environment Variables
# Copy this file to .env and fill in real values
# NEVER commit real credentials to git

PORT=3000
NODE_ENV=development

# Database (replace with your actual database URL)
DATABASE_URL=postgresql://user:password@localhost:5432/docrolds

# Authentication
JWT_SECRET=generate-a-random-64-char-string
JWT_CUSTOMER_SECRET=generate-another-random-64-char-string
ADMIN_USERNAME=admin@yourdomain.com
ADMIN_PASSWORD=use-a-strong-password

# Square Payment (use sandbox for testing)
SQUARE_APPLICATION_ID=sandbox-your-app-id
SQUARE_ACCESS_TOKEN=your-square-access-token
SQUARE_LOCATION_ID=your-square-location-id
SQUARE_ENVIRONMENT=sandbox

# Square Webhooks (optional - enables signature verification for refunds/disputes)
# Must match the notification URL configured in the Square dashboard exactly.
SQUARE_WEBHOOK_SIGNATURE_KEY=
SQUARE_WEBHOOK_NOTIFICATION_URL=https://api.docrolds.com/api/webhooks/square

# Email - tried in this order: Brevo, then SendGrid, then Gmail SMTP.
# Brevo is recommended: free tier (300/day) with no trial expiration,
# sent over HTTPS. Gmail SMTP is blocked outbound on most cloud hosts
# (Render included) and is only useful for local development.
BREVO_API_KEY=xkeysib-your-api-key
BREVO_FROM_EMAIL=info@yourdomain.com
BREVO_FROM_NAME=Your App Name

SENDGRID_API_KEY=SG.your-api-key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=Your App Name

EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASS=your-gmail-app-password

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# Download Settings
DOWNLOAD_LINK_EXPIRY_DAYS=7
